# React Agent Chainlit Template

A template for creating ReAct agents using langgraph. Provides a chainlit frontent with streaming support.

## Installation

```{bash}
python -m pip install poetry
poetry install
```

## How to run

```{bash}
chainlit run cl_app.py
```
